import{_ as a,o as i,c as t,ag as s}from"./chunks/framework.ClKCtVAA.js";const u=JSON.parse('{"title":"Releasing vscode-reveal","description":"","frontmatter":{},"headers":[],"relativePath":"releasing.md","filePath":"releasing.md","lastUpdated":1785598928000}'),n={name:"releasing.md"};function r(l,e,o,d,h,p){return i(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="releasing-vscode-reveal" tabindex="-1">Releasing vscode-reveal <a class="header-anchor" href="#releasing-vscode-reveal" aria-label="Permalink to &quot;Releasing vscode-reveal&quot;">​</a></h1><p>The release workflow publishes one VSIX artifact to GitHub Releases, the Visual Studio Marketplace, and Open VSX. It defaults to a dry run so maintainers can inspect the exact artifact before any registry is changed.</p><h2 id="_1-prepare-the-release" tabindex="-1">1. Prepare the release <a class="header-anchor" href="#_1-prepare-the-release" aria-label="Permalink to &quot;1. Prepare the release&quot;">​</a></h2><ol><li><p>Triage or explicitly defer open security and compatibility pull requests.</p></li><li><p>Move the relevant entries under <code>Unreleased</code> in <code>CHANGELOG.md</code> to a section matching the intended semantic version.</p></li><li><p>Run <code>npm version &lt;version&gt; --no-git-tag-version</code> to update the version in both <code>package.json</code> and <code>package-lock.json</code> consistently.</p></li><li><p>Run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ci</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> coverage</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @vscode/vsce</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-dependencies</span></span></code></pre></div></li><li><p>Install the VSIX in a clean supported VS Code profile and verify preview, browser launch, HTML export, and the PDF flow with <code>sample.md</code>.</p></li></ol><h2 id="_2-run-the-safe-dry-run" tabindex="-1">2. Run the safe dry run <a class="header-anchor" href="#_2-run-the-safe-dry-run" aria-label="Permalink to &quot;2. Run the safe dry run&quot;">​</a></h2><p>Dispatch the <strong>Release</strong> workflow with <code>dryRun</code> enabled. The workflow builds the production bundle, creates the VSIX, verifies that it contains the extension manifest, bundle, and changelog, and uploads it as an artifact. It does not contact a registry.</p><p>Download and inspect that artifact before continuing. The version must not already have a <code>release/&lt;version&gt;</code> tag.</p><h2 id="_3-publish-the-verified-artifact" tabindex="-1">3. Publish the verified artifact <a class="header-anchor" href="#_3-publish-the-verified-artifact" aria-label="Permalink to &quot;3. Publish the verified artifact&quot;">​</a></h2><p>Dispatch the workflow again with <code>dryRun</code> disabled. Keep all three publish targets enabled unless a registry is intentionally being recovered independently:</p><ul><li>Visual Studio Marketplace</li><li>Open VSX</li><li>GitHub Releases</li></ul><p>The protected GitHub environments should require a maintainer approval and hold their own publishing token.</p><h2 id="_4-verify-publication" tabindex="-1">4. Verify publication <a class="header-anchor" href="#_4-verify-publication" aria-label="Permalink to &quot;4. Verify publication&quot;">​</a></h2><p>Confirm that the same version is visible in all three locations and that a fresh installation works:</p><ul><li><code>https://marketplace.visualstudio.com/items?itemName=evilz.vscode-reveal</code></li><li><code>https://open-vsx.org/extension/evilz/vscode-reveal</code></li><li><code>https://github.com/evilz/vscode-reveal/releases</code></li></ul><p>If one registry fails, rerun only that target with the others disabled. Never rebuild or modify the VSIX between registry publications for the same version.</p>`,15)])])}const v=a(n,[["render",r]]);export{u as __pageData,v as default};
